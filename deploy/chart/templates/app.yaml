apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    {{- include "portal.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: backend
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: 8000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    {{- include "portal.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.app.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: backend
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: backend
        app.kubernetes.io/instance: {{ .Release.Name }}
      annotations:
        checksum/secrets: {{ toJson .Values.secrets | sha256sum }}
        {{- if .Values.observability.enabled }}
        checksum/otel-sidecar-config: {{ toJson .Values.observability | sha256sum }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.images.backend.repository }}:{{ .Values.images.backend.tag }}"
          imagePullPolicy: {{ .Values.images.backend.pullPolicy }}
          env:
            - {name: REDMINE_BASE_URL, value: "http://redmine:3000"}
            - name: REDMINE_API_KEY
              valueFrom:
                secretKeyRef: {name: support-ticket-portal-secrets, key: REDMINE_API_KEY}
            - {name: REDMINE_PROJECT_ID, value: {{ .Values.app.redmineProjectId | quote }}}
            - {name: REDMINE_TRACKER_ID, value: {{ .Values.app.redmineTrackerId | quote }}}
            - {name: REDIS_URL, value: "redis://redis:6379/0"}
            - {name: SESSION_COOKIE_SECURE, value: {{ .Values.app.sessionCookieSecure | quote }}}
            - {name: CORS_ORIGINS, value: {{ printf "%s://%s" (ternary "https" "http" .Values.ingress.tls.enabled) (include "portal.host" .) | quote }}}
            {{- if .Values.observability.enabled }}
            - {name: LOG_LEVEL, value: "DEBUG"}
            - {name: OTEL_EXPORTER_OTLP_ENDPOINT, value: "http://localhost:4318"}
            - {name: OTEL_EXPORTER_OTLP_PROTOCOL, value: "http/protobuf"}
            - {name: OTEL_RESOURCE_ATTRIBUTES, value: {{ printf "deployment.environment.name=%s" .Values.environment | quote }}}
            {{- else }}
            - {name: OTEL_SDK_DISABLED, value: "true"}
            {{- end }}
            {{- range $name, $value := .Values.app.backendEnv }}
            - name: {{ $name }}
              value: {{ $value | toString | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources: {{ toYaml .Values.resources.backend | nindent 12 }}
        {{- if .Values.observability.enabled }}
        - name: otel-collector
          image: "{{ .Values.images.otelCollector.repository }}:{{ .Values.images.otelCollector.tag }}"
          imagePullPolicy: {{ .Values.images.otelCollector.pullPolicy }}
          args: ["--config=/etc/otelcol/config.yaml"]
          ports:
            - {name: otlp-http, containerPort: 4318}
            - {name: health, containerPort: 13133}
          readinessProbe:
            httpGet: {path: /, port: health}
            initialDelaySeconds: 2
            periodSeconds: 10
          resources: {{ toYaml .Values.resources.otelCollector | nindent 12 }}
          volumeMounts:
            - {name: otel-sidecar-config, mountPath: /etc/otelcol}
        {{- end }}
      {{- if .Values.observability.enabled }}
      volumes:
        - name: otel-sidecar-config
          configMap: {name: backend-otel-collector-config}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    {{- include "portal.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    {{- include "portal.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.app.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: frontend
          image: "{{ .Values.images.frontend.repository }}:{{ .Values.images.frontend.tag }}"
          imagePullPolicy: {{ .Values.images.frontend.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet: {path: /, port: http}
            periodSeconds: 10
          resources: {{ toYaml .Values.resources.frontend | nindent 12 }}
