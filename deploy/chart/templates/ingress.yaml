{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: support-ticket-portal
  labels:
    {{- include "portal.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations: {{ toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts: [{{ include "portal.host" . | quote }}]
      secretName: {{ required "ingress.tls.secretName is required when TLS is enabled" .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ include "portal.host" . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port: {name: http}
{{- end }}
---
{{- if .Values.backendIngress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend
  labels:
    {{- include "portal.labels" . | nindent 4 }}
  {{- with .Values.backendIngress.annotations }}
  annotations: {{ toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.backendIngress.className }}
  {{- if .Values.backendIngress.tls.enabled }}
  tls:
    - hosts: [{{ include "portal.backendHost" . | quote }}]
      secretName: {{ required "backendIngress.tls.secretName is required when TLS is enabled" .Values.backendIngress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ include "portal.backendHost" . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend
                port: {name: http}
{{- end }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: redmine
  labels:
    {{- include "portal.labels" . | nindent 4 }}
  {{- with .Values.redmineIngress.annotations }}
  annotations: {{ toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.redmineIngress.className }}
  {{- if .Values.redmineIngress.tls.enabled }}
  tls:
    - hosts: [{{ include "portal.redmineHost" . | quote }}]
      secretName: {{ required "redmineIngress.tls.secretName is required when TLS is enabled" .Values.redmineIngress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ include "portal.redmineHost" . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: redmine
                port: {name: http}
