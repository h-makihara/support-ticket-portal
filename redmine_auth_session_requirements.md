# Redmine 認証・セッション管理 追加実装要件

## 1. 目的

Redmine を基幹システムとして利用する既存の Frontend / Backend
構成に、ログイン認証およびセッション管理機能を追加する。

認証そのものは Redmine に委譲し、Frontend / Backend は Redmine
の背後にある認証基盤を直接意識しない構成とする。

Redmine 側でローカル認証、LDAP、Microsoft Active Directory（LDAP
連携）などが利用されている場合も、Backend は Redmine
に対してユーザー名・パスワードによる認証を試行し、その結果のみを扱う。

ログイン成功後は Redmine の API キーを Backend 側の Redis
に保持し、Frontend には API キーを公開しない。

------------------------------------------------------------------------

## 2. 基本アーキテクチャ

``` text
[Frontend]
    |
    | username / password
    | POST /auth/login
    v
[Backend]
    |
    | Redmine に認証を委譲
    | username / password
    v
[Redmine]
    |
    +-- Redmine ローカル認証
    +-- LDAP
    +-- Microsoft Active Directory（LDAP）
    +-- その他 Redmine 側で構成された対応認証
```

ログイン成功後は以下の構成とする。

``` text
[Frontend]
    |
    | Session ID
    v
[Backend]
    |
    | Session ID から Redis を参照
    v
[Redis]
    |
    | Redmine API Key を取得
    v
[Backend]
    |
    | Redmine API Key
    v
[Redmine REST API]
```

Frontend から Redmine REST API を直接呼び出さないこと。

------------------------------------------------------------------------

## 3. 認証に関する責務

### Frontend

Frontend は以下のみを担当する。

-   独自にブランディングされたログイン画面を表示する
-   ユーザー名とパスワードを Backend に送信する
-   ログイン状態を Backend のセッション API で確認する
-   未ログインの場合はログイン画面へ遷移する
-   ログイン済みの場合は通常のアプリケーション画面を表示する
-   Redmine の API キーを保持・参照しない
-   Redmine に直接ログインリクエストを送信しない

### Backend

Backend は以下を担当する。

-   Frontend からユーザー名・パスワードを受け取る
-   Redmine に認証を委譲する
-   認証成功時に対象ユーザーの Redmine API キーを取得する
-   暗号学的に安全なランダムな Session ID を発行する
-   Session ID と Redmine API キー等のセッション情報を Redis に保存する
-   Redis のセッション TTL を 6 時間に設定する
-   Frontend からのリクエストに含まれる Session ID を検証する
-   Session ID に対応する Redmine API キーを利用して Redmine REST API
    を操作する
-   ログアウト時に Redis のセッションを削除する

### Redmine

Redmine は認証方式の解決を担当する。

Backend は Redmine
の背後で利用されている認証方式を原則として意識しないこと。

例えば Redmine が Microsoft Active Directory を LDAP
認証先として利用している場合、認証フローは以下となる。

``` text
Frontend
    |
    | username / password
    v
Backend
    |
    | username / password
    v
Redmine
    |
    | LDAP authentication
    v
Microsoft Active Directory
```

Backend が Microsoft Active Directory
に直接認証を行う実装にはしないこと。

------------------------------------------------------------------------

## 4. ログインフロー

以下のフローを実装する。

``` text
1. Frontend が username / password を入力
2. Frontend が POST /auth/login を Backend に送信
3. Backend が Redmine に username / password で認証を試行
4. Redmine が設定済みの認証方式でユーザーを認証
5. Backend が認証成功を確認
6. Backend が対象ユーザーの Redmine API キーを取得
7. Backend が Session ID を生成
8. Backend が Redis にセッション情報を保存
9. Redis に TTL 6時間を設定
10. Backend が Frontend にセッションを確立
11. 以降 Frontend は Session ID を利用して Backend と通信
12. Backend は Session ID から API キーを取得して Redmine REST API を操作
```

認証失敗時にはセッションを作成しないこと。

------------------------------------------------------------------------

## 5. セッション管理

Redis をセッションストアとして使用する。

### TTL

セッションの TTL は以下とする。

``` text
6 hours = 21,600 seconds
```

初期実装では、ログイン時から 6 時間で失効する固定 TTL を基本とする。

既存コードの設計上 Sliding Expiration
が適切な場合でも、勝手に仕様変更せず、必要であれば実装前または実装結果で明示すること。

### Redis のデータ例

概念的には以下の情報を保持する。

``` json
{
  "redmineUserId": 123,
  "username": "user01",
  "redmineApiKey": "xxxxxxxxxxxxxxxx",
  "createdAt": "ISO-8601 datetime"
}
```

Redis キーの例：

``` text
session:<session-id>
```

API キーは機密情報として扱うこと。

既存システムに暗号化基盤や Secret
管理の仕組みが存在する場合は、それを優先して利用すること。過剰な独自暗号化機構を新規導入する場合は、その理由を明示すること。

------------------------------------------------------------------------

## 6. Session ID の受け渡し

Web Frontend を前提として、原則として Session ID は Cookie で管理する。

推奨属性：

``` text
HttpOnly
Secure
SameSite=Lax
Path=/
```

Frontend の JavaScript から Session ID
を直接読み取る必要がない構成を優先すること。

Cookie
名は既存プロジェクトの命名規則に合わせること。適切な規則が存在しない場合は、例えば以下を使用できる。

``` text
session_id
```

本番環境では `Secure` を必須とする。

Frontend と Backend が異なる Origin
で動作する場合は、既存のデプロイ構成を確認し、CORS、Cookie の
Domain、SameSite、credentials 設定を適切に構成すること。

------------------------------------------------------------------------

## 7. 必要な認証 API

既存のルーティング規約に合わせ、少なくとも以下に相当する API
を実装する。

### POST /auth/login

ログイン処理を行う。

入力例：

``` json
{
  "username": "user01",
  "password": "password"
}
```

処理：

1.  入力値を検証
2.  Redmine に認証を委譲
3.  認証成功を確認
4.  Redmine API キーを取得
5.  Session ID を生成
6.  Redis にセッション情報を保存
7.  TTL 21,600 秒を設定
8.  Session Cookie を返却

パスワードは Redis、DB、ログ等に保存しないこと。

### GET /auth/session

現在のログイン状態を返す。

ログイン済みの場合の例：

``` json
{
  "authenticated": true,
  "user": {
    "id": 123,
    "username": "user01",
    "name": "User Name"
  }
}
```

未ログインまたはセッション期限切れの場合は、既存 API
のエラーレスポンス規約に従って `401 Unauthorized` を返すこと。

API キーはレスポンスに含めないこと。

### POST /auth/logout

ログアウト処理を行う。

処理：

1.  Session ID を特定
2.  Redis から該当セッションを削除
3.  Session Cookie を失効
4.  正常レスポンスを返却

------------------------------------------------------------------------

## 8. Redmine API 呼び出し

ログイン後の Redmine 操作は、すべて Backend 経由とする。

``` text
Frontend
    |
    | Session Cookie
    v
Backend
    |
    | Redis lookup
    v
Redmine API Key
    |
    v
Redmine REST API
```

既存のチケット作成、コメント追加等の Redmine API
呼び出し処理についても、固定 API キーや Frontend から渡された API
キーを利用するのではなく、ログインユーザーのセッションから取得した API
キーを利用できるように変更すること。

Redmine への API キー送信方式は、既存実装および Redmine REST API
の仕様に合わせること。

------------------------------------------------------------------------

## 9. Frontend のログイン制御

Frontend 起動時または認証が必要な画面へのアクセス時に、Backend
のセッション確認 API を呼び出す。

概念的なフロー：

``` text
Application Start
    |
    v
GET /auth/session
    |
    +-- 200 / authenticated
    |       |
    |       v
    |   Application
    |
    +-- 401
            |
            v
        Login Page
```

ログイン画面は Redmine の UI を利用せず、Frontend
側で独自に実装・ブランディングできる構成とする。

ログイン成功後はアプリケーション画面へ遷移する。

セッション期限切れを Backend が検出して `401` を返した場合は、Frontend
側でログイン画面へ戻せるようにする。

------------------------------------------------------------------------

## 10. セキュリティ要件

以下を必須要件とする。

-   ユーザーのパスワードを永続化しない
-   ユーザーのパスワードを Redis に保存しない
-   ユーザーのパスワードをログ出力しない
-   Redmine API キーを Frontend に返さない
-   Redmine API キーをログ出力しない
-   Session ID は十分なエントロピーを持つ安全な乱数で生成する
-   Session ID の固定化攻撃を防止するため、ログイン成功時に新規 Session
    ID を発行する
-   Cookie には HttpOnly を設定する
-   本番環境では Cookie に Secure を設定する
-   認証エラー時に、ユーザーの存在有無を過度に推測できるエラーメッセージを返さない
-   Redmine への認証通信および API 通信は HTTPS を前提とする
-   Redis を外部へ直接公開しない
-   Redis へのアクセス制御は既存インフラのセキュリティ方針に従う

既存プロジェクトに CSRF 対策が存在する場合はそれを利用すること。

Cookie ベース認証を新規導入することで CSRF
対策が必要になる場合は、既存アーキテクチャに適した対策を実装すること。

------------------------------------------------------------------------

## 11. 認証方式に関する前提

今回の実装では、Redmine 側の認証方式を Backend から抽象化する。

想定対象：

-   Redmine ローカル認証
-   LDAP 認証
-   Microsoft Active Directory を LDAP として利用する認証

ただし、以下のような認証方式については、ユーザー名・パスワードによる
Redmine REST API 認証が利用できない可能性がある。

-   SAML
-   OpenID Connect
-   Microsoft Entra ID による SSO
-   ADFS
-   Kerberos / Windows 統合認証
-   Redmine の独自認証プラグイン

今回の実装では、現在利用する Redmine 環境において「Backend
からユーザー名・パスワードで Redmine に認証し、対象ユーザーの API
キーを取得できること」を確認すること。

この前提が成立しない場合は、無理に実装を変更せず、原因と代替案を報告すること。

------------------------------------------------------------------------

## 12. Codex への実装指示

既存コードベースを確認した上で、上記要件を既存アーキテクチャ・コーディング規約に沿って実装すること。

以下の方針を守ること。

1.  まず既存の Frontend / Backend の構成を調査する
2.  既存の Redmine API クライアント実装を確認する
3.  既存の認証・セッション・Redis 関連実装があれば再利用する
4.  不要な大規模リファクタリングは行わない
5.  Redmine API の既存機能を壊さない
6.  固定 API キーを使用している箇所があれば、ユーザーセッション由来の
    API キーへ移行する
7.  認証処理を適切なサービス層・モジュールへ分離する
8.  エラーハンドリングを既存規約に合わせる
9.  必要な環境変数・設定値を追加する
10. `.env.example` 等が存在する場合は更新する
11. 必要なテストを追加・更新する
12. README または既存ドキュメントにセットアップ方法を追記する

実装後は以下を報告すること。

-   変更したファイル一覧
-   実装した認証フロー
-   Redis のセッション構造
-   追加した API
-   Frontend の認証制御方法
-   追加・変更した環境変数
-   実行したテストと結果
-   未解決事項または環境依存事項
-   Redmine の認証方式に関する前提条件

------------------------------------------------------------------------

## 13. 受け入れ条件

以下を満たした場合に実装完了とする。

-   独自 Frontend のログイン画面からログインできる
-   Frontend から Redmine へ直接認証情報を送信していない
-   Backend が Redmine に認証を委譲している
-   Redmine 側の LDAP / Active Directory 認証を Backend
    が直接実装していない
-   認証成功後に Redmine API キーを取得できる
-   パスワードを保存していない
-   API キーが Frontend に露出していない
-   Session ID が安全に発行される
-   Redis にセッションが保存される
-   Redis セッションの TTL が 6 時間である
-   Frontend が Session ID に基づいて Backend API を利用できる
-   Backend がセッションに紐づく API キーで Redmine を操作できる
-   未ログイン時にログイン画面が表示される
-   セッション期限切れ時に再ログインへ誘導される
-   ログアウトによって Redis セッションが削除される
-   既存のチケット作成・コメント追加機能がログインユーザーの権限で正常に動作する
-   認証情報・API キーがログに出力されない

---

## 14. 検証用ローカルアカウントの作成

デリバリ時に、Redmine のローカル認証を利用する検証用アカウントとして、以下の3種類のユーザーを作成すること。

- 管理者ユーザー
- サポートユーザー
- 営業担当ユーザー

これらは LDAP / Microsoft Active Directory などの外部認証には依存せず、Redmine のローカルアカウントとしてログイン可能な状態にすること。

アカウント名、初期パスワード、メールアドレス等は既存プロジェクトの命名規則やデリバリ方式に合わせること。固定の認証情報をソースコードへ直接ハードコードしないこと。

初期認証情報を環境変数、初期化スクリプト、Seed、Fixture 等で設定する場合は、以下を満たすこと。

- 本番環境へ意図せず作成されないこと
- 検証環境または明示的に有効化された環境でのみ作成されること
- 初期パスワードをアプリケーションログへ出力しないこと
- デリバリ時に検証担当者が確認できる方法で認証情報を提供できること
- 必要に応じて初回ログイン後のパスワード変更手順をドキュメント化すること

各ユーザーには、検証に必要な範囲で Redmine 上の適切なロール・権限を設定すること。

想定する役割は以下とする。

### 管理者ユーザー

以下を検証できる権限を持つこと。

- Redmine 管理者として必要な操作
- Frontend / Backend 経由でのログイン
- セッション作成
- Redmine API を利用したチケット操作
- 管理者権限が必要な機能の確認

### サポートユーザー

以下を検証できる権限を持つこと。

- Frontend / Backend 経由でのログイン
- 担当範囲のチケット参照
- チケット作成
- コメント追加
- サポート担当者向けに許可された更新操作
- 許可されていない管理者向け操作が実行できないこと

### 営業担当ユーザー

以下を検証できる権限を持つこと。

- Frontend / Backend 経由でのログイン
- 営業担当者向けに許可されたチケット参照・作成・更新
- コメント追加
- サポートまたは管理者専用の操作が実行できないこと

実際のロール名および権限設定は、既存の Redmine プロジェクト・ロール設計に合わせること。

---

## 15. 検証用アカウントの検証手順

上記3アカウントについて、デリバリ時に以下の検証を実施できること。

### 共通ログイン検証

各アカウントで以下を確認する。

1. Frontend のログイン画面を表示する
2. 検証対象ユーザーのユーザー名・パスワードを入力する
3. Frontend から Backend のログイン API が呼ばれることを確認する
4. Backend が Redmine に認証を委譲していることを確認する
5. ログイン成功後に Redis にセッションが作成されることを確認する
6. Redis セッションの TTL が 21,600 秒、または実行時点から概ね6時間であることを確認する
7. Frontend に Redmine API キーが返却されていないことを確認する
8. Session ID を利用して Backend API を呼び出せることを確認する
9. Backend がセッションに紐づく Redmine API キーを利用して Redmine API を呼び出せることを確認する
10. ログアウト後に Redis セッションが削除されることを確認する
11. セッション期限切れ後に `401 Unauthorized` となり、再ログインへ誘導されることを確認する

### 管理者ユーザーの検証

以下を確認する。

- 正常にログインできること
- チケットを作成できること
- コメントを追加できること
- 管理者として許可された操作を実行できること
- Backend 経由の操作が Redmine 上で当該管理者ユーザーとして記録されること

### サポートユーザーの検証

以下を確認する。

- 正常にログインできること
- チケットを参照できること
- チケットを作成できること
- コメントを追加できること
- サポート担当者として許可された更新操作を実行できること
- 管理者専用操作が拒否されること
- Backend 経由の操作が Redmine 上で当該サポートユーザーとして記録されること

### 営業担当ユーザーの検証

以下を確認する。

- 正常にログインできること
- 営業担当者として許可されたチケット操作を実行できること
- コメントを追加できること
- サポートまたは管理者専用の操作が拒否されること
- Backend 経由の操作が Redmine 上で当該営業担当ユーザーとして記録されること

### 権限差分の検証

3アカウントで同一操作を試行し、Redmine 側に設定されたロール・権限の差分が Backend 経由でも正しく反映されることを確認する。

Backend 側で Redmine の権限モデルを独自に再実装しないこと。

原則として、操作可否は各セッションに紐づく Redmine API キーと Redmine 側の権限設定によって決定される構成とする。

---

## 16. ドキュメント追加要件

既存の README、セットアップ手順書、デリバリ手順書、検証手順書等のうち適切なドキュメントに、以下を追記すること。

- 検証用ローカルアカウントとして3ユーザーを作成すること
- 管理者ユーザー、サポートユーザー、営業担当ユーザーそれぞれの用途
- 各ユーザーに設定する Redmine のロール・権限
- 検証環境でのアカウント作成方法
- 初期認証情報の確認方法
- Frontend からのログイン手順
- Redis セッション作成の確認方法
- セッション TTL の確認方法
- ログアウト時のセッション削除確認方法
- セッション期限切れ時の挙動確認方法
- 各アカウントでのチケット作成・コメント追加・更新操作の検証方法
- アカウント間の権限差分を確認する方法
- Backend 経由の操作が、それぞれの Redmine ユーザーとして記録されることの確認方法

実際のユーザー名や初期パスワードをドキュメントへ記載する場合は、対象環境のセキュリティ方針に従うこと。

公開リポジトリや第三者が閲覧可能なドキュメントへ実パスワードを記載しないこと。

---

## 17. 追加の受け入れ条件

既存の受け入れ条件に加えて、以下を満たすこと。

- デリバリ時に管理者・サポートユーザー・営業担当ユーザーの3種類の検証用ローカルアカウントを作成できる
- 3アカウントが Redmine のローカル認証でログインできる
- 3アカウントすべてが Frontend / Backend 経由で正常にログインできる
- 3アカウントそれぞれについて Redis セッションが作成される
- 各セッションの TTL が6時間である
- 各アカウントの Redmine API キーが Frontend に露出しない
- Backend 経由の Redmine 操作が、それぞれログイン中のユーザーとして実行される
- 管理者・サポート・営業担当の権限差分が Redmine 側の設定に従って正しく反映される
- サポートユーザーおよび営業担当ユーザーが、許可されていない操作を実行できない
- 3アカウントの作成方法と検証方法がドキュメント化されている
- デリバリ担当者または検証担当者が、ドキュメントのみを参照して一連の認証・セッション・権限検証を実施できる
